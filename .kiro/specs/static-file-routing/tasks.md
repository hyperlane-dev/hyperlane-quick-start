# 实现计划

- [x] 1. 创建静态文件配置模块
  - 在 `config/business/` 目录下创建 `static_files` 模块
  - 定义静态文件服务相关的常量（目录路径、缓存配置等）
  - 创建配置结构体和默认值
  - _需求: 1.1, 3.1, 4.1, 5.3_

- [x] 2. 实现静态文件数据模型
  - 在 `app/model/business/` 目录下创建 `static_files` 模块
  - 定义 `StaticFileResponse` 结构体用于封装文件响应数据
  - 定义 `StaticFileError` 枚举用于错误处理
  - 实现相关的 trait（如 Display, Debug 等）
  - _需求: 2.1, 2.2, 2.3, 2.4_

- [x] 3. 实现路径安全验证功能
  - 在 `app/service/static_files/` 目录下创建路径验证模块
  - 实现 `validate_path_security` 函数防止路径遍历攻击
  - 实现路径规范化和边界检查
  - 编写路径安全验证的单元测试
  - _需求: 5.1, 5.2, 5.3, 5.4_

- [x] 4. 实现文件类型检测和 Content-Type 设置
  - 在静态文件服务中集成现有的 `FileExtension` 工具类
  - 实现 `get_content_type_for_file` 函数
  - 支持常见文件类型的 Content-Type 映射（CSS、JS、HTML、图片等）
  - 为未知类型设置默认 Content-Type
  - _需求: 3.1, 3.2, 3.3, 3.4, 3.5_

- [x] 5. 实现核心静态文件服务功能
  - 创建 `app/service/static_files/` 模块
  - 实现 `serve_resource_file` 函数处理文件读取
  - 集成路径验证和文件类型检测
  - 实现异步文件读取和错误处理
  - _需求: 1.1, 1.2, 1.3, 1.4, 2.1_

- [x] 6. 实现缓存机制和条件请求处理
  - 在静态文件服务中添加 ETag 生成功能
  - 实现 Last-Modified 头设置
  - 添加 Cache-Control 头配置
  - 实现 If-Modified-Since 和 If-None-Match 条件请求处理
  - _需求: 4.1, 4.2, 4.3, 4.4_

- [x] 7. 创建静态文件控制器
  - 在 `app/controller/` 目录下创建 `static_files` 模块
  - 实现 `/static/{path:.*}` 路由处理函数
  - 集成静态文件服务调用
  - 实现完整的错误响应处理（404、403、400、500）
  - _需求: 1.1, 1.2, 1.3, 1.4, 2.1, 2.2, 2.3, 2.4_

- [x] 8. 配置路由注册和模块导出
  - 在 `app/controller/mod.rs` 中添加 `static_files` 模块导入
  - 在 `app/lib.rs` 中导出静态文件相关类型
  - 确保路由正确注册到 Hyperlane 框架
  - 验证与现有路由的兼容性
  - _需求: 1.1_

- [x] 9. 编写单元测试
  - 为路径验证功能编写测试用例
  - 为文件服务功能编写测试用例
  - 为缓存机制编写测试用例
  - 为错误处理编写测试用例
  - _需求: 2.1, 2.2, 2.3, 2.4, 5.1, 5.2, 5.3, 5.4_

- [x] 10. 编写集成测试
  - 创建端到端路由测试
  - 测试各种文件类型的正确服务
  - 测试安全防护机制
  - 测试缓存和条件请求功能
  - _需求: 1.1, 1.2, 1.3, 1.4, 3.1, 3.2, 3.3, 3.4, 4.1, 4.2, 4.3, 4.4_